import tkinter as tk
from tkinter import filedialog

def create_gui():
    window = tk.Tk()
    window.title("Meme Generator")

    # Add widgets: buttons, labels, entry fields
    # Logic to open image, get text, and call `generate_meme`

    window.mainloop()

create_gui()
